apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.auth.dns }}
  namespace: {{ .Values.global.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.auth.dns }}
  template:
    metadata:
      labels:
        app: {{ .Values.auth.dns }}
    spec:
      containers:
        - name: {{ .Values.auth.dns }}
          image: {{ .Values.auth.image }}:{{ .Values.global.tag }}
          ports:
            - containerPort: {{ .Values.auth.port }}
          envFrom:
            - configMapRef:
                name: host-services-config
