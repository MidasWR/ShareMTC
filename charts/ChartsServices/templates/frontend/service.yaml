apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.frontend.dns }}
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: {{ .Values.frontend.dns }}
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
